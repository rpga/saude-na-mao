<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helth"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:background="@drawable/marca"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.Health" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Hospital"
            android:background="@drawable/marca"
            android:label="@string/title_activity_hospital" >
        </activity>
        <activity
            android:name=".Localidade"
            android:background="@drawable/marca"
            android:label="@string/title_activity_localidade" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value="com.example.Localidade" />
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".Especialidade"
            android:background="@drawable/marca"
            android:label="@string/title_activity_especialidade" >
        </activity>
        <activity
            android:name=".Sobre"
            android:background="@drawable/marca"
            android:label="@string/title_activity_sobre" >
        </activity>
        <activity
            android:name=".ListaUDS"
            android:label="@string/title_activity_lista_uds" >
        </activity>
        <activity
            android:name=".Especialidade2"
            android:label="@string/title_activity_especialidade2" >
        </activity>
    </application>

</manifest>
